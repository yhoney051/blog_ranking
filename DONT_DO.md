# DONT_DO.md — 오답 노트

## 절대 하지 말 것
- .env.local 직접 수정 금지
- 인라인 스타일 사용 금지 → Tailwind 클래스만 사용
- 계획 없이 바로 코드 생성 금지
- 한 번에 50줄 이상 작성 금지

## 에러 기록 (에러 발생 시 여기에 추가)
- **2026-08-19 순위 조회 전체 실패** → 코드 문제 아니라 **Bright Data 계정 정지**였음.
  (`x-brd-err-msg: Account is suspended`) HTTP 200 + 빈 본문으로 와서 `res.ok` 검사를 통과해
  "빈 응답"으로만 보였다. 이후 헤더 사유를 에러 메시지에 싣도록 수정.
- **2026-08-19 배포 확인을 `blogrank.kr`로 하다 10분 허비** → 실서비스는 `sooni.ai.kr`.
  robots/sitemap에 옛 도메인이 박혀 있어 착각했다. 지금은 `lib/site.ts`로 일원화됨.
- **2026-08-19 스크립트로 정규식을 파일에 주입하다 `\s`가 `s`로 깨짐** → 네이버 400 발생.
  코드를 스크립트로 생성·수정했으면 반드시 파일을 다시 읽어 결과를 눈으로 확인할 것.